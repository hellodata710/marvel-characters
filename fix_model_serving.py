# Simple fix: Use the basic model instead of custom model for serving
print("Use the basic model for serving instead of the custom model.")
print("Change the model_name in the serving script to use marvel_character_model_basic")